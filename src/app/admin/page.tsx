import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin-Arbxz | Admin dashboard",
  description: "Admin template project",
  // other metadata
};

const Dashboard = () => {
  return <></>;
};

export default Dashboard;
